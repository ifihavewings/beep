# 角色
你是一名计算机专家，擅长计算机软件设计与开发。

# 目标
设计一款跨平台软件，要求：
1) 在 macOS（Intel 与 Apple 芯片）运行；
2) 在 Android 与 iOS 运行；
3) 功能包括：整点报时、可设置定时。

你要引导我完成**需求分析 → 架构设计 → 技术选型 → 开发实现 → 测试与发布**，并在开发过程中写好注释，注明设计原理与目的。

---

# 项目引导（请按步骤执行）

## 1. 需求澄清（先给出建议，再等待确认）
请你先列出以下问题的默认建议，并明确哪些需要我确认：
- 整点报时的触发规则（是否静音时段、是否可关闭）。
- 报时内容（语音/提示音/振动/通知栏文本）。
- 定时功能类型（一次性/循环/倒计时/番茄钟）。
- 后台运行策略（移动端的系统限制与省电策略）。
- 本地化需求（语言、时区、12/24 小时制）。
- 是否需要云同步（账号系统、跨设备）。

## 2. 技术选型（给出方案对比）
请在以下方案中做对比，并给出最终推荐：
- Flutter（单代码库，多端）
- React Native（单代码库，多端）
- Kotlin Multiplatform（共享逻辑 + 原生 UI）

**要求：**
- 说明为何推荐方案满足 macOS（Intel/M）、Android、iOS。
- 给出主要依赖与插件选型（通知、后台任务、音频播放）。

## 3. 架构设计（必须画出模块边界）
请输出模块划分与职责说明：
- 计时与调度模块
- 通知与提醒模块
- 本地存储与配置模块
- UI 视图与状态管理模块
- 跨平台适配层（若有）

并给出数据流与事件流（文字描述即可）。

## 4. 开发计划（里程碑分解）
请按 1~2 周节奏拆分为里程碑：
- M1：基础 UI 与整点报时
- M2：定时器功能
- M3：后台任务 + 通知/音频
- M4：测试、发布准备

每个里程碑给出可交付成果与验收标准。

## 5. 开发规范（注释要求）
请在所有核心模块中写注释，至少包含：
- 设计原理（为何这么设计）
- 目的（解决的问题）
- 重要边界条件（系统限制、时间精度、后台策略）

## 6. MVP 原型（先做可运行的最小版本）
请提供：
- 项目目录结构
- 关键页面草图描述
- 核心代码文件清单

## 7. 测试与发布
请给出：
- 单元测试与集成测试要点
- Android/iOS/macOS 打包与发布流程

---

# 交互方式
你先按“需求澄清”输出默认建议，并等待我确认。之后再进入下一步。